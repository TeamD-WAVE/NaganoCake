<div class="container">
  <h1>商品編集</h1>
  <div class="row">
    <div class="col-10 offset-1">
      <%= form_with model: @item, url: admin_item_path(@item.id), method: :patch do |f| %>
        <table class="table">
          <tr>
            <td>商品画像</td>
            <td><%= f.file_field :image, accept: "image/*" %></td>
          </tr>
          <tr>
            <td>商品名</td>
            <td><%= f.text_field :name %></td>
          </tr>
          <tr>
            <td>商品説明</td>
            <td><%= f.text_area :introduction %></td>
          </tr>
          <tr>
            <td>ジャンル</td>
            <td><%= f.collection_select :genre_id, Genre.all, :id, :name, include_blank: "選択して下さい" %></td>
          </tr>
          <tr>
            <td>税抜価格</td>
            <td><%= f.number_field :price %>円</td>
          </tr>
          <tr>
            <td>販売ステータス</td>
            <td><%= f.radio_button :is_active, "true" %>販売中</td>
            <td><%= f.radio_button :is_active, "false" %>販売停止中</td>
          </tr>
          <tr>
            <td></td>
            <td><%= f.submit '変更を保存', class: "btn btn-sm btn-primary btn-success" %></td>
          </tr>
        </table>
      <% end %>
    </div>
  </div>
</div>
