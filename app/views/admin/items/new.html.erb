<h1>商品新規登録</h1>
  <%= form_with model: @post_image do |f| %>
    <h4>商品画像</h4>
    <%= f.file_field :image, accept: "image/*" %>
    <h4>商品名</h4>
    <%= f.text_field :name %>
    <h4>商品説明</h4>
    <%= f.text_area :caption %>
    <h4>ジャンル</h4>
    <h4>税抜</h4>
    <%= f.submit '投稿' %>
  <% end %>
