<div class="genre_search">
  <table class="table">
    <tr>
      <th>ジャンル検索</th>
    </tr>

    <%#= form_with url: genre_search_path, method: :get do |f| %>
      <% Genre.all.each do |genre| %>
        <tr>
          <td><%= link_to genre.name, "/public/genres/#{genre.id}/search/", method: :get %></td>
        </tr>
      <% end %>
    <%# end %>

  </table>
</div>